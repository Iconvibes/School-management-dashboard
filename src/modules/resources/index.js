/**
 * Resources Module — Manages schemes of work and class resources (notes,
 * assignments, reading materials).
 *
 * Store functions: createSchemeOfWork, getSchemesOfWork, getSchemeOfWork,
 *   updateSchemeOfWork, deleteSchemeOfWork, createClassResource,
 *   listClassResources, getClassResource, updateClassResource, deleteClassResource
 * API routes: /api/scheme/*, /api/resources/*
 * Components: SchemeOfWorkTab, SchemeView, ResourcesTab, ResourcesView
 * Models: SchemeOfWork, ClassResource
 */
export * from "./store";
